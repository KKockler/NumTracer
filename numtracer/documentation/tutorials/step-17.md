# step-17: Per-flavour and per-component dressings

*Builds on: [step-02](step-02.md) · Built on by: — · Tags: `sun`, `dressing` · **Tier A***

```{admonition} This page is not written yet
:class: warning
The tutorial series is being written in order. This step is planned and its scope is
fixed, but the narrative below is an outline rather than a finished page.
```

## What this step will cover

* Dressing u and d differently *inside* one SU(N) trace, with no per-flavour diagram split.
* `ntSUNDiagFund` / `ntSUNDiagAdj` and the `{c -> name, …, Default -> def}` spec.
* The two invariants: COLLAPSE (all dressings equal recovers the blind fold) and DROP (a dropped component == a zero dressing).
* A gluon condensate on the SU(3) Cartan directions.

## Where the material lives in the meantime

Until this page is written, read these directly:

* `Tutorials/step-17-dressed-flavour/dressed_flavour.cpp` (runnable now: `ctest -R dressed_flavour`)
