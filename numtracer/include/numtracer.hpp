/// @file numtracer.hpp
/// @brief The public umbrella header: `#include <numtracer.hpp>` pulls in the whole NumTracer API.
///        A root-level forwarder — the content lives in `numtracer/numtracer.hpp`.
#pragma once
#include <numtracer/numtracer.hpp> // IWYU pragma: export
